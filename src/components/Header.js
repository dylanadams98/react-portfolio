import React from "react";

function Header({ currentPage }) {
    return (
        <header>
        <header>
      <h4 contenteditable="helvetica">Dylan Adams</h4>
    </header>
       
      </header>
    );
}

export default Header;